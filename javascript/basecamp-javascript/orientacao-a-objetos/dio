let lines = gets().split("\n");

let line = lines.shift().split('soma');
let soma = 0

//TODO: Complete os espaços em branco com uma possível solução para o desafio

for (i = 0; i < line; i++) {
    let transfor = parseInt(line[soma])
    soma = soma + line
}
print(soma)